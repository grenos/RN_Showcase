import {StyleSheet} from 'react-native';

export const s = StyleSheet.create({
  searchButton: {
    borderRadius: 10,
    marginBottom: 30,
    backgroundColor: '#d0b51b',
  },
  // the following styles would normally be in a centralized theme or a storybook
  fullWidth: {
    width: '100%',
  },
  pdTop20: {
    paddingTop: 20,
  },
  pdBottom30: {
    paddingBottom: 30,
  },
  pd20: {padding: 20},
});
